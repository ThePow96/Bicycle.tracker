<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bicycletracker">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.FindUsActivity"
            android:label="@string/title_activity_find_us"/>
        <activity
            android:name=".view.MenuActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/AppTheme.NoActionBar"
            android:name=".view.ContactActivity" />
        <activity
            android:theme="@style/AppTheme.NoActionBar"
            android:name=".view.TrackMenuActivity" />
        <activity
            android:theme="@style/AppTheme.NoActionBar"
            android:name=".view.SpecialPlacesActivity" />
        <activity
            android:theme="@style/AppTheme.NoActionBar"
            android:name=".view.SettingsActivity" />
    </application>

</manifest>