<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--teks kontakt -->
    <style name="TextViewContact" parent="Widget.AppCompat.TextView">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_view_contact</item>
        <item name="android:layout_marginTop">@dimen/container_margin</item>
    </style>
    <!-- styl tytuł-->
    <style name="title" parent="Widget.AppCompat.TextView">
        <item name="android:gravity">right</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:layout_marginBottom">@dimen/title_margin</item>
        <item name="android:layout_marginRight">@dimen/title_margin</item>
    </style>
    <!-- styl przycisków menu -->
    <style name="ButtonMenu" parent="Widget.AppCompat.TextView">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_view_contact</item>
        <item name="android:layout_marginTop">@dimen/container_margin</item>
    </style>
    <!--Styl w menu tras -->
    <style name="SectionViewSize" parent="Widget.AppCompat.TextView">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:textSize">@dimen/section_view_size</item>
        <item name="android:layout_marginTop">@dimen/section_margin</item>
    </style>
</resources>
